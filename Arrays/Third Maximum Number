class Solution {
    public int thirdMax(int[] nums) {
        int max= nums[0];
        int[]
        for(int i=1;i<nums.length;i++){
            if(max<nums[i]){
                max=nums[i];   
            }
        }
        return max3;
    }
}
